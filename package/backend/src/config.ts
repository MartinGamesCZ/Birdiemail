// App configuration
export const IS_DEV = process.env.NODE_ENV === 'development'; // Check if the app is in development mode
export const PUBLIC_WEB_URL = process.env.NET_WEB_PUB ?? ''; // Public web URL
